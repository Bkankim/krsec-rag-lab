# RAG 미니 프로젝트 계획 v2 (Astra xhigh 검수 반영)

- 2026-09-19. v1(마일스톤 M0~M6 초안)을 Codex gpt-6-astra(xhigh) 적대적 검수에 넣고, 지적을 수용/기각 판정해 반영한 판.
- 원본 검수 보고: 세션 로그. v1 계획서: 세션 scratchpad `rag-project-plan-for-review.md`.

## 배경 (v1과 동일)

- BK: 보안관제 출신, 온프렘 LLM 엔지니어 전직 준비. 업스테이지 면접(2026-09-16)에서 임베딩 플로우 질문에 무너짐. 갭 = 데이터 레이어(파싱→청킹→임베딩→적재→검색)의 실체.
- 목적: GitHub 공개 프로젝트(계정 Bkankim)로 갭을 실험으로 메우고 증명. 블로그 시리즈로 마무리.
- 컨셉: "한국어 보안 문서 RAG 평가 하네스 - 온프렘 스택 ablation 리포트". 챗봇 UI는 부록.

## 검수 핵심 지적과 판정

1. **CVE ID 자동 채점은 약한 라벨이다 - 수용 (최중요).** CVE가 제목에만 있는 청크를 회수해도 채점은 성공하지만 근거 검색은 실패일 수 있다. "어노테이션 비용을 줄이는 방법과 생략하는 방법을 혼동하지 말 것." → CVE 조인은 후보 생성용. 사람 검수 골든셋(질문별 관련 문서 + 근거 위치 + 판정 이유)을 따로 만들고, 자동 라벨의 오류율을 먼저 측정한다.
2. **M0가 갭을 직접 검증하지 않는다 - 수용.** numpy 코사인만으론 부족. 문서 1건·질의 1건의 전 단계 추적(토큰화·길이 제한 → pooling → 정규화 → shape/dtype/norm → point ID·payload → 검색 점수 → 회수 원문)을 요구하고, NumPy 전수 계산 vs Qdrant exact vs HNSW 근사를 대조·구분한다.
3. **LangGraph 과투자 - 부분 수용.** 검색·평가 코어는 프레임워크 없는 함수 + CLI로 먼저 완성, LangChain/LangGraph는 통합 마일스톤으로 후치(채택 자체는 유지 - 시장 요구, BK 결정).
4. **M1이 M2를 지연시킨다 - 수용.** 소스 5개 전부 수집하지 말고 KISA+NVD 소표본 고정 fixture로 먼저 평가에 도달. Sigma·ATT&CK는 질문 유형·정답 정의가 다르므로 별도 질문 유형이 생겼을 때 추가. KEV는 코퍼스가 아니라 표본 선택·메타데이터 보강용으로 먼저 판단.
5. **M3 4축은 독립이 아니다 - 수용.** 청킹은 임베딩 입력·BM25 통계·리랭커 입력에 영향. 공통 기준선에서 한 요소씩 바꾸는 비교와 누적 개선을 구분, 각 실행의 기준 설정 명시. 파싱 실험과 청킹 실험 분리. HWP/PDF 표 처리는 실패 사례로 필요성 입증 후 범위 확정.
6. **실험 종료 조건 = 수치 표가 아니라 가설 판정 + 근거 - 수용.** 악화·무효과 실험도 종료 가능. 어떤 질문에서 악화됐는지 질문별 결과 필수.
7. **평가 정의 선행 - 수용.** 정답 단위(문서/청크/CVE) 결정, hit@k vs recall@k 구분, dev/test 분할(공지·CVE·근접중복 묶음 기준), 질의 유형 4종 분리(CVE 명시 / 제품·증상 / 설명·비교 / 답 없음), exact CVE-ID lookup을 베이스라인에 포함, 지표 코드는 수계산 사례와 교차 검증, ANN recall과 relevance recall 분리.
8. **한영 실험 설계 - 수용.** KO→EN은 영어 근거 회수를 명시 요구, 동일 정보요구 EN→EN 대조군, CVE 포함/미포함 분리 보고.
9. **BM25 조건 고정 - 수용.** 한국어 토큰화·CVE 하이픈 처리·후보 수·RRF 설정 명시. bge-m3는 "dense 출력만 사용"을 명시(sparse/multi-vector와 혼동 금지). 임베딩 교체 시 revision·입력형식·정밀도 기록.
10. **WSL 드라이버 위치 교정 - 수용.** Windows 호스트 드라이버만 설치, WSL 내부엔 Linux 디스플레이 드라이버 설치 금지(CUDA 툴킷만). vLLM 버전에 맞는 Python·PyTorch·CUDA 조합 고정.
11. **Tailscale 호출 ≠ air-gap 증거 - 수용.** 폐쇄망 주장은 외부 차단 환경에서 오프라인 설치·실행을 검증했을 때만. 그 전엔 "온프렘 서빙"까지만 주장.
12. **라이선스 완료 선언 성급 - 수용.** KISA 정책 원문 미확보 상태. 노트북 출력·검색 결과 예시·평가 로그·블로그 인용으로 본문이 새는 경로 점검. Sigma DRL은 author·원본 링크·고지 보존을 데이터 실물에 연결. 소스별 정책 URL·확인일 기록.
13. **축당 블로그 1편 의무 - 부분 수용.** 의무 해제, 소재가 되는 실험만 글로. 블로그 마무리 목표 자체는 유지(BK 결정).
14. **README의 "선례 0건" 류 표현 - 수용.** 조사 범위·시점 한정으로 서술.
15. **추가 3건 - 전부 수용.** ① 갱신·삭제·재인덱싱 정확성 검증(문서 ID·버전·콘텐츠 해시, 재적재 멱등성) ② 재현 패키지(manifest·해시·수집 시점·모델 revision·질문별 원시 결과, 공개 fixture로 기본 평가 실행 가능, README 표는 원시 결과에서 재생성) ③ 실패 분석 분류(파싱 손실/청크 경계/임베딩 오검색/후보 누락/리랭킹 역전/생성 오류) + 최종 게이트 = BK 본인이 처음부터 재실행하고 실패 질의 1건의 단계별 입출력을 설명.

## 마일스톤 v2

| M | 내용 | Exit 조건 |
|---|---|---|
| M0 | 데이터 레이어 통과의례 | 문서 1건·질의 1건 전 단계 추적 기록(토큰화→pooling→정규화→적재→점수→원문) + NumPy 전수 vs Qdrant exact vs HNSW 대조 + **BK가 추적 기록을 본인 말로 설명(내재화 게이트)** |
| M1 | 미니 코퍼스 + 평가 규약 | KISA+NVD 소표본 고정 fixture / 평가 단위·정답 기준·질의 유형 4종·dev/test 분할 문서화 / CVE 자동 라벨 오류율 측정 / 사람 검수 골든셋 v1(근거 위치 포함) |
| M2 | 베이스라인 3종 + 첫 릴리스 | exact CVE-ID lookup / BM25(토큰화 규칙 고정) / dense(bge-m3 dense only) - 질문별 결과, 지표 수계산 교차검증, 재현 명령 포함 릴리스 v0.1 |
| M3 | 실험 시리즈 (공통 기준선, 한 요소씩) | 하이브리드+RRF → 리랭커(동일 후보군, 후보 recall 병기) → 청킹(파싱 고정) → 한영(KO→EN 명시, EN→EN 대조) → 임베딩 교체(재인덱싱). 각 실험 exit = 가설 판정 + 실패 사례 분석. M2 직후 생성단 thin-slice(소수 질문) 연결 |
| M4 | 코퍼스 확장 + 운영 검증 | KISA 전체 수집(누락·중복·파싱 실패 계측), 갱신·삭제·재인덱싱 멱등성 검증, (질문 유형 정의되면) Sigma·ATT&CK 추가 |
| M5 | 통합 + 생성 평가 | LangChain/LangGraph 통합(코어는 함수/CLI 유지), faithfulness+정답성+답없음 처리 분리 판정, top-k 실험(입력 토큰 수 기록, 검색 실패 vs 생성 실패 분리) |
| M6 (선택) | 온프렘 서빙 | bk-pc GPU 확인(→ 초기로 이동, 다음 세션) 후 WSL2+CUDA(호스트 드라이버만)+vLLM, 하드웨어별 결과 분리, air-gap 주장은 오프라인 설치 검증 시에만 |

- 릴리스는 M2부터 마일스톤마다(중도 취업 시 완결성). 최종 게이트: BK 본인 재실행 + 실패 질의 1건 단계별 설명.
- 운영: 이슈=작업 단위, PR=이슈 닫기(수치 변화 외에 라벨 수정·재현성 보완·원인 규명 PR도 정상), 블로그는 소재 있는 실험만.
