# PLAN

한국어 보안 문서 RAG 평가 하네스 - 온프렘 스택 ablation 리포트.

## 목적

RAG 파이프라인의 데이터 레이어(파싱→청킹→임베딩→적재→검색)를 프레임워크 추상화 없이
직접 구현하고, 구성요소별 효과를 검증된 평가셋 위에서 측정한 기록을 남긴다.
"돌아가는 데모"가 아니라 "왜 이 설계인지 수치로 답할 수 있는 상태"가 목표다.

## 설계 원칙

1. **자동 라벨은 후보일 뿐이다.** CVE ID 조인으로 만든 정답 후보는 약한 라벨로 취급한다.
   사람 검수 골든셋(근거 위치·판정 이유 포함)을 따로 두고, 자동 라벨의 오류율을 먼저 측정한다.
2. **한 요소씩 바꾼다.** 청킹·검색·리랭킹·임베딩은 서로 독립이 아니다. 공통 기준선에서
   한 요소씩 바꾸는 비교와 누적 개선을 구분하고, 각 실행의 기준 설정을 명시한다.
3. **실험의 종료는 수치 표가 아니라 가설 판정이다.** 기각된 가설도 정상 종료다
   (`hypothesis-rejected` 라벨). 어떤 질문에서 악화됐는지 질문별 결과를 남긴다.
4. **평가 정의가 실험보다 먼저다.** 정답 단위(문서/청크/CVE), hit@k vs recall@k,
   질의 유형 분리(CVE 명시 / 제품·증상 / 설명·비교 / 답 없음), dev/test 분할,
   ANN recall과 relevance recall의 구분을 실험 전에 문서로 고정한다.
5. **재현 가능해야 한다.** 데이터 manifest(해시·수집 시점), 모델 revision, 설정,
   질문별 원시 결과를 릴리스에 남기고, README의 표는 원시 결과에서 재생성한다.
6. **라이선스가 코퍼스 구성을 결정한다.** 재배포 조건이 확인되지 않은 원문(KISA 공지 본문)은
   리포·릴리스·로그·노트북 출력 어디에도 넣지 않는다. 상세는 [LICENSES/](LICENSES/).
7. **온프렘으로 돌아간다.** 로컬 임베딩(bge-m3) + Qdrant + 로컬 LLM. 폐쇄망(air-gap) 관련
   주장은 실제로 외부 차단 환경에서 검증한 범위까지만 한다.

## 마일스톤

| M | 내용 | Exit 조건 |
|---|---|---|
| M0 | 데이터 레이어 통과의례 | 문서 1건·질의 1건의 전 단계 추적(토큰화→pooling→정규화→적재 point ID·payload→검색 점수→회수 원문) + NumPy 전수 vs Qdrant exact vs HNSW 대조 |
| M1 | 미니 코퍼스 + 평가 규약 | KISA+NVD 소표본 고정 fixture / 평가 규약 문서화 / CVE 자동 라벨 오류율 측정 / 사람 검수 골든셋 v1 |
| M2 | 베이스라인 3종 + 첫 릴리스 | exact CVE-ID lookup / BM25(토큰화 규칙 고정) / dense(bge-m3 dense only) - 질문별 결과, 지표 교차검증, 재현 릴리스 v0.1 |
| M3 | 실험 시리즈 | 하이브리드+RRF → 리랭커(동일 후보군) → 청킹(파싱 고정) → 한영 교차(KO→EN 명시) → 임베딩 교체. 각 실험 = 가설 판정 + 실패 사례 분석 |
| M4 | 코퍼스 확장 + 운영 검증 | KISA 전체 수집(누락·중복·파싱 실패 계측), 갱신·삭제·재인덱싱 멱등성 검증 |
| M5 | 통합 + 생성 평가 | LangChain/LangGraph 통합(코어는 함수/CLI 유지), faithfulness·정답성·답없음 처리 분리 판정 |
| M6 (선택) | 온프렘 서빙 | WSL2+CUDA+vLLM 구축, 하드웨어별 결과 분리, 처리량·VRAM 실측 |

세부 완료 기준은 각 [마일스톤](../../milestones)과 이슈에 있다.
릴리스는 M2부터 마일스톤마다 낸다 - 어느 시점에 멈추더라도 그때까지가 완결된 산출물이 되도록.
